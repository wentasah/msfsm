project('msfsm', 'cpp',
        default_options: [
          'cpp_std=c++11',
          'buildtype=debugoptimized',
        ])

#gtest_dep = dependency('gtest', main : true, required : true)

subdir('examples')
